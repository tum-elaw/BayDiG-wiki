import { SystemCommonProps, SystemTypographyProps } from './constants';
import { SxProp } from './sx';
import { ComponentProps } from './utils/types';
declare type StyledLinkProps = {
    hoverColor?: string;
    muted?: boolean;
    underline?: boolean;
} & SystemCommonProps & SxProp & SystemTypographyProps;
declare const Link: import("styled-components").StyledComponent<"a", any, StyledLinkProps, never>;
export declare type LinkProps = ComponentProps<typeof Link>;
export default Link;
