import { MaxWidthProps } from 'styled-system';
import { SystemCommonProps, SystemTypographyProps } from './constants';
import { SxProp } from './sx';
import { ComponentProps } from './utils/types';
declare type StyledTruncateProps = {
    title: string;
    inline?: boolean;
    expandable?: boolean;
} & MaxWidthProps & SystemTypographyProps & SystemCommonProps & SxProp;
declare const Truncate: import("styled-components").StyledComponent<"div", any, StyledTruncateProps, never>;
export declare type TruncateProps = ComponentProps<typeof Truncate>;
export default Truncate;
